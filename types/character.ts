export interface CharacterRecord {
    id: string,
    ownerId: string,
    pages: CharacterPage[],
    activePage: number // Default to 0
}

export interface CharacterPage {
    
}

export interface CharacterRecord {

}

